# pnpm 11 moved workspace + install settings out of package.json.
# Single-package repo, but this file is where onlyBuiltDependencies and
# overrides now live.

onlyBuiltDependencies:
  - sharp
  - unrs-resolver

overrides:
  postcss@<8.5.10: '>=8.5.10'
